function ChannelIdx = max_peak_channel(PeriodicPeaks, FrequencyRange)
% ChannelIdx = max_peak_channel(PeriodicPeaks, FrequencyRange)
% Identifies the channel with the highest amplitude periodic peak within a
% range. 

% TODO